# Copy to .env and fill values locally. Do not commit .env.

# OpenCode model aliases
OPENCODE_MODEL_FAST=minimax-m2.1
OPENCODE_MODEL_STRONG=openai-codex-latest
OPENCODE_MODEL_CHEAP=gemini-2.0-flash

# Provider credentials placeholders
OPENAI_OAUTH_PROFILE=default
CODEX_OAUTH_PROFILE=default
MINIMAX_API_KEY=
GEMINI_API_KEY=

# Include MCP vars from mcp/.env.example in this file as needed,
# or keep them in a separate sourced file.
